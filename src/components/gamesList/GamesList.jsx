import React from 'react';
import SiteHeader from '../shared/siteHeader/SiteHeader';

function GamesList(props) {
  return (
    <div className="games-list">
      <SiteHeader />This is the games list.
    </div>
  );
}

export default GamesList;
